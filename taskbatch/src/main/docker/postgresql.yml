version: '2'
services:
  taskbatch-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/taskbatch/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=taskbatch
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
